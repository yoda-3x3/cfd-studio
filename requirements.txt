numpy==2.4.6
scipy==1.18.0
matplotlib==3.11.1
PySide6==6.11.1
numba==0.66.0
trimesh==4.12.2
rtree==1.4.1
fast-simplification==0.1.13
manifold3d==3.5.2
shapely==2.1.2
embreex==4.4.0

# No extra pip packages needed to build the optional native kernel
# backend (solver3d/build_native.py) -- it's a plain g++-compiled shared
# library loaded via ctypes (stdlib), not a Python extension module, so
# there's no wrapper-generation build step. Just needs a C++ compiler
# (g++) on PATH.
