# CMake build output
build/
build-*/

# vcpkg's local install tree for this manifest (headers/libs it downloads
# and builds) -- machine-specific, regenerated by `vcpkg install`, not
# source.
vcpkg_installed/

# CLion / other IDE CMake caches, if ever opened that way
cmake-build-*/
