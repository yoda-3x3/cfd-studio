// Placeholder for Phase 5's cfd_headless CLI (full numerics-only pipeline:
// both solvers, both flow-domain modes, OpenFOAM + VTK export), exercised
// end-to-end before any GUI work starts.
#include <cstdio>

int main() {
    std::puts("cfd_headless: skeleton only, Phase 5 not started yet.");
    return 0;
}
